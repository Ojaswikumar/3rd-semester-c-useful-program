#include<bits/stdc++.h>
using namespace std;
bool check(string &str)
{
    stack<char> s;
    char ch;
    int i;
    for(i = 0;i<str.lenght();i++)
    {
        if(s.empty())
        {
            s.push(str[i]);
        } 
        else if()
    }
}